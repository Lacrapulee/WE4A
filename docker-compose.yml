services:
  db:
    image: mysql:8.0
    # ... gardez votre config mysql ...

  php:
    build: ./docker/php
    ports:
      - "8080:80" # On expose directement le port d'Apache
    volumes:
      - ./src:/var/www/html
    depends_on:
      - db
      
